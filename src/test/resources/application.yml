server:
  port: 8087

spring:
  profiles:
    default: local
  datasource:
    url: jdbc:h2:mem:~/lectures
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
